
const internetPackage = 100; // in megabytes
const usedData = 80; // in megabytes

if (usedData < internetPackage) {
  console.log("You have used less than 100 megabytes of your internet package.");
}
